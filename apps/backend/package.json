{
  "name": "@tallynest/backend",
  "version": "0.0.0",
  "private": true,
  "description": "TallyNest AWS Lambda backend",
  "type": "module",
  "engines": {
    "node": ">=24.14.0 <25"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.6.5"
  }
}
